# 第一次用 GitHub 协作

## 第一次加入

1. 注册 GitHub 账号，把用户名发给仓库管理员；管理员会把你邀请为协作者。
2. 安装 GitHub Desktop：<https://desktop.github.com/>。
3. 在 GitHub Desktop 选择 **File → Clone repository**，选中 `robotcon-summer-camp-2026`，保存到自己的工作目录。不要直接在 QQ 里反复传同名 CAD 文件。

## 每次开始前

1. 打开 GitHub Desktop，点击 **Fetch origin**，如果出现 **Pull origin** 就先拉取。
2. 在群里说清楚自己要改的零件或装配体；同一时间只让一人修改同一个 CAD 文件。

## 每次完成后

1. 确认 SolidWorks 文件已经保存并能重新打开。
2. 在 GitHub Desktop 的左侧检查改动；不要把临时文件、软件安装包或无关资料一起勾选。
3. 填写一句明确的提交说明，例如：`底盘：双侧夹持铝管孔位`，点击 **Commit to main**。
4. 点击 **Push origin**。推送成功后，再在群里说“已推送”。

## 遇到冲突

- 先停下，不要点击“保留我的版本”覆盖别人。
- 把 GitHub Desktop 的冲突提示截图发到群里，并联系最后修改同一文件的人。
- CAD 不能像 Word 一样自动合并；通常应保留双方文件、人工比较后再决定哪一版进入装配体。

## 当前约定

- 以仓库 `main` 分支为当前共享版本。
- 每个可验证的小改动单独提交；提交说明写“部件 + 改动”，不要只写“更新”。
- 大型参考库（顶层 `——` 目录）和本地软件不会上传；需要时由管理员另行提供来源。

